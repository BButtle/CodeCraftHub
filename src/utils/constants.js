const USER_ROLES = Object.freeze({
  USER: "user",
  ADMIN: "admin"
});

const DIFFICULTY = Object.freeze(["beginner", "intermediate", "advanced"]);
const PACE = Object.freeze(["slow", "moderate", "fast"]);

module.exports = { USER_ROLES, DIFFICULTY, PACE };
